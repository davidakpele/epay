# Upstream Configuration
# Single monolith — all traffic proxies to epay-server:8080

upstream epay_backend {
    least_conn;
    server epay-server:8080 max_fails=3 fail_timeout=30s;
    keepalive 32;
    keepalive_requests 1000;
    keepalive_timeout 60s;
}
